import { StyleSheet } from 'react-native';

const cardProps = StyleSheet.create({
  root: {
    borderRadius: 6,
    elevation: 2,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.5,
    shadowRadius: 2
  }
});

export default cardProps;
